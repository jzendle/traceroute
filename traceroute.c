#include "traceroute.h"

#include <stdlib.h>
#include <stdio.h>

/*
	Perform a Traceroute to Supplied Address
*/
int perform_traceroute(const char * address) {
	/* This function needs some content. */
	printf("tracing route to '%s'\n", address);

	return EXIT_FAILURE;
}
